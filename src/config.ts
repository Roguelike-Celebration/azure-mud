// TODO: Eventually abstract this out of this repo
// My .env trick I've used in other projects needs some touching up of its regex
export default {
  SERVER_HOSTNAME: "https://roguelike-mud.azurewebsites.net",
};
